---
id: plugin
title: Plugin
description: Plugin
slug: /classes/plugin
sidebar_label: Plugin
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## Table of contents

| <i className="fa-solid fa-boxes-stacked"/> Methods |
| :------------------------------------------------- |
| [load()](#load)                                    |
| [unload()](#unload)                                |

## Methods

### load()

> Load the plugin.
>
> Returns: [void](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/void)
>
> | <i className="fa-solid fa-hashtag"/> Parameter | <i className="fa-solid fa-font"/> Type | <i className="fa-solid fa-list"/> Description |
> | :--------------------------------------------- | :------------------------------------- | :-------------------------------------------- |
> | `manager`                                      | [`Manager`](./Manager.mdx)             | Manager                                       |

### unload()

> Unload the plugin.
>
> Returns: [void](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/void)
>
> | <i className="fa-solid fa-hashtag"/> Parameter | <i className="fa-solid fa-font"/> Type | <i className="fa-solid fa-list"/> Description |
> | :--------------------------------------------- | :------------------------------------- | :-------------------------------------------- |
> | `manager`                                      | [`Manager`](./Manager.mdx)             | Manager                                       |

## Example usage

<Tabs>
    <TabItem value="usage" label="Using plugins" default>
        ```js
        const { Manager } = require("magmastream");
        const MyPlugin = require("my-magmastream-plugin");

        const manager = new Manager({
            ...ManagerOptions,
            plugins: [new MyPlugin({ foo: "bar" })],
        });
        ```
    </TabItem>
    <TabItem value="writing" label="Writing plugins" default>
        ```js
        const { Plugin } = require("magmastream");

        module.exports = class MyPlugin extends Plugin {
            constructor(options) {
                super();
                this.options = options; // will be { foo: 'bar' }
            }

            load(manager) {}
        };
        ```
    </TabItem>

</Tabs>
